<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ratemyfoodapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RegisterScreen"></activity>
        <activity android:name=".AddMealScreen" />
        <activity android:name=".ProfileScreen" />
        <activity android:name=".SearchDScreen" />
        <activity android:name=".FoodGScreen" />
        <activity android:name=".GuestScreen" />
        <activity android:name=".HostScreen" />
        <activity android:name=".MainScreen" />
        <activity android:name=".LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>